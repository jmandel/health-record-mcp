import React from 'react'
import ReactDOM from 'react-dom/client'
import AgentTester from './AgentTester' // Import the new component
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AgentTester />
  </React.StrictMode>,
) 